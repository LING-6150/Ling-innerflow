apiVersion: 1

datasources:
  - name: Tempo
    type: tempo
    access: proxy
    url: http://tempo:3200
    editable: true
    jsonData:
      # Link trace spans to Prometheus metrics (exemplars / service graph)
      tracesToMetrics:
        datasourceUid: prometheus
      nodeGraph:
        enabled: true
